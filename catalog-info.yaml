apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: example
  description: example.com
  annotations:
    backstage.io/techdocs-ref: dir:. # Path to the directory containing mkdocs.yml
spec:
  type: website
  lifecycle: production
  owner: user:guest